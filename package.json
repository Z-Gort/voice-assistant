{
  "name": "gmail-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Gmail AI application with mobile frontend and backend",
  "packageManager": "pnpm@9.15.2",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint",
    "typecheck": "pnpm --recursive typecheck",
    "format": "pnpm --recursive format:write",
    "format:check": "pnpm --recursive format:check"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "prettier": "^3.3.2",
    "typescript": "~5.8.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
